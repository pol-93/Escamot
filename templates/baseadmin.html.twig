<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %} Escamot {% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
         <meta name="viewport" content="width=device-width, user-scalable=no">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        
        
        {{include('svg/svgtemplate.html.twig')}}
        {% block header %}
            <header>
                <div class="container">
                    <div class="row">
                        <div class="col-1">
                            <img src="{{ asset('build/images/logo.webp') }}" alt="escamot logo" class="main-logo">
                        </div>
                        <div class="col">
                            <nav>
                                <ul class="menu">
                                    <li><a href="{{ path('success') }}">Editar Manifest</a></li>
                                    <li><a href="{{ path('Blog') }}">Blog</a></li>
                                    <li><a href="{{ path('Newsletter') }}">Newsletter</a></li>
                                    <li><a href="{{ path('admin') }}">Administració d'usuaris</a></li>
                                    <li><a href="{{ path('logout') }}">tancar sessió</a></li>
                                </ul>
                            </nav>
                            
                        </div>
                    </div>
                </div>
            </header>
        {% endblock %}
        {% block body %}
        {% endblock %}
    </body>
</html>
